import 'package:flutter/material.dart';

class CardStudentWidget extends StatelessWidget {
  const CardStudentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
