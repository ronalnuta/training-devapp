import 'package:flutter/material.dart';

class WorkScheduleWidget extends StatelessWidget {
  const WorkScheduleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
